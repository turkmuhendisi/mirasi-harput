# Android yerel gizli ayarlar (opsiyonel)
# Bu dosyayı secrets.properties olarak kopyalayın — git'e gitmez.
#
# Sertifika API anahtarı backend .env içindeki API_KEY ile aynı olmalı.
# ApiConfig.kt içindeki API_KEY alanına da aynı değeri yazın.
#
# cert.api.key=your_api_key_here
